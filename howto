Force rebuild package :
touch packages/[pkg]/build or scripts/build ** --force

build/build.host/kconfig-frontends-3.12.0.0/.config=current build file (copy to config/defconfigs/base-rpi-*)

ldd :
$TOOLCHAINDIR/build/../toolchain/bin/armv6-openbricks-linux-gnueabi-readelf -a [file_to_ldd] | grep "Shared library:"
